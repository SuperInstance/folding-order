[package]
name = "folding-order"
version = "0.1.0"
edition = "2021"
description = "5-stage RG flow for temporal anomaly detection"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
